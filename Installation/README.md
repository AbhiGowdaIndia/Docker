# Docker Installation

### Install Git on Windows

* Download Git from the official site:

👉 https://git-scm.com/download/win

* Run the installer and use default options (recommended).

* When asked:

  * Editor → Vim (or choose VS Code if you prefer)

  * PATH environment → Git from the command line and 3rd-party software

  * HTTPS backend → OpenSSL

* Finish installation.

* Verify installation:

```cmd
git --version
```

### Install Git on Linux

### Ubuntu / Debian

'''cmd
sudo apt update
sudo apt install git -y
```

### RHEL / CentOS / Amazon Linux

```cmd
sudo yum install git -y
```

* Verify:

```cmd
git --version
```
